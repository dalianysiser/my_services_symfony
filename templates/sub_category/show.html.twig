{% extends 'base.html.twig' %}

{% block title %}SubCategory{% endblock %}

{% block body %}
<div class="container">
    <h1>SubCategory</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Name</th>
                <td>{{ sub_category.name }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ sub_category.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sub_category_index') }}">back to list</a>

    <a href="{{ path('app_sub_category_edit', {'id': sub_category.id}) }}" class="btn btn-outline-primary">edit</a>
    <br><br>
    {{ include('sub_category/_delete_form.html.twig') }}
</div>
{% endblock %}
